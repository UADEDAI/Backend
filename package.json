{
  "devDependencies": {
    "husky": "^8.0.3"
  },
  "scripts": {
    "prepare": "husky install",
    "doc:build": "swagger-cli bundle docs/index.yaml --outfile docs/swagger.json",
    "doc:serve": "npx http-server docs/ --cors"
  }
}
