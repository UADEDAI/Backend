{
  "devDependencies": {
    "husky": "^8.0.3"
  },
  "scripts": {
    "prepare": "husky install",
    "doc:build": "swagger-cli bundle docs/index.yaml --outfile docs/swagger.json",
    "doc:serve": "swagger-cli bundle docs/index.yaml --outfile docs/swagger.json && npx http-server docs/ --cors -c-1"
  }
}
