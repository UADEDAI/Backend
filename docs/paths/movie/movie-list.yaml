tags :
  - Movie
summary: Get all the Movies
description: Get all the Movies
produces:
  - application/json
responses:
  200:
    description: Movies fetched successfully
    schema:
      type: array
      items:
        $ref: ../../definitions/Movie.yaml
  400:
    description: Error in fetching Movies
    schema:
      type: object
      $ref: ../../definitions/CommonResponse.yaml